使用gpt3写的代码，输入提示：
帮我写一个cmd程序，让它读取当前文件夹的文件夹名，再读取当前文件夹中的文件名，然后按顺序存到txt文档中，cmd程序自己的名字除外，还要只把每个文件夹内部的一级子文件夹名字也都取出来，然后在txt文档中，在父文件夹名字的下一行缩进3个空格然后写上去，一步一步思考，然后再写代码

我看结果，很多子文件夹的文件名都被写到了txt，我只要子文件夹的名字

那以此类推，你继续写一个取用三级文件夹的代码吧

放在文件夹里，双击就好了